import { Box, Heading, Text } from '@chakra-ui/react';

const LocalNews = () => (
  <Box p={4}>
    <Heading mb={4}>Local News</Heading>
    <Text fontSize="lg">Stay updated with the latest local news.</Text>
  </Box>
);

export default LocalNews;